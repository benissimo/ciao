[project]
name = "ciao"
version = "0.1.0"
description = "A simple app that says hello in random languages"
requires-python = ">=3.8"
dependencies = [
    "flask>=3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
